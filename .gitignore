# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.g.dart
*.freezed.dart

# Platform folders non nécessaires (app Android uniquement, cf. android/ ci-dessous)
/ios/
/linux/
/macos/
/windows/
/web/

# Android : versionné pour piloter le build CI (minSdk, Firebase App
# Distribution), à l'exception des fichiers générés/locaux ci-dessous.
android/.gradle/
android/local.properties
android/key.properties
android/app/build/
android/build/
android/**/*.iml

# IDE
.idea/
.vscode/
*.iml

# Environnement / secrets
.env
*.env.local

# Supabase CLI local
supabase/.branches
supabase/.temp
